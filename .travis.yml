matrix:
    include:
        - language: node_js
          node_js:
              - 12
          before_script:
              - cd frontend
          script:
              - npm ci
              - npm run build:ci

        - language: java
          jdk: oraclejdk11
          services:
              - mysql
          env: INFINITAG_USER=root INFINITAG_PASSWORD=
          script:
              - ./mvnw package
